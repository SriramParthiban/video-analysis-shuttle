// Renders the real login screen (the /login route itself redirects to /dashboard
// via proxy.ts, so this mirror lets us view it). TEMPORARY — safe to delete.
export { default } from '@/app/login/page';
